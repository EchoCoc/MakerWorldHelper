# Desktop V3 TODO

这个文档用于记录本地资源库桌面端的第三版性能优化方向，后续可以继续往下补充。

## 目标

- 让项目很多时的启动速度更快
- 让后台刷新更偏向增量而不是全量重建
- 让列表页和详情页都按“按需加载”思路继续减压

## 当前已完成基础

- 启动优先读取本地缓存快照
- 后台再执行真实扫描
- 项目级刷新已支持单独处理
- 扫描阶段已支持基于项目指纹复用未变化项目
- 项目列表已加入分页
- 图片资源已补充缓存友好响应头和懒加载

## 第三版待办

### 1. 目录级增量索引

- 为根目录建立更细粒度的项目索引文件
- 记录每个项目目录的最近修改时间、关键文件签名、仓库归属
- 启动时先扫描目录结构和关键时间戳，不立刻深读所有项目
- 只对新增、删除、移动、修改过的项目做深度解析

### 2. 详情数据延迟加载

- 列表页只保留卡片所需最小字段
- `summaryHtml`、`pictureItems`、`instanceItems` 改成进入详情页后再加载
- 项目描述、详情图、打印配置拆成按需请求
- 详情页首次打开先显示骨架屏，再补完整内容

### 3. 图片与文件索引继续瘦身

- 列表页优先只使用封面图，不提前准备全部详情图
- Plate 缩略图和实例封面在悬停或展开时再加载
- 模型文件列表延后到用户点击箭头或右键时再准备

### 4. 后台刷新体验优化

- 增加“后台刷新中”但不阻塞操作的状态提示
- 刷新后只局部替换变化项目，不整页抖动
- 如果当前详情页项目未变化，不重新打断用户阅读位置

### 5. 缓存策略完善

- 区分“列表缓存”和“详情缓存”
- 为缓存加过期策略和版本迁移策略
- 当缓存结构升级时自动安全失效重建

### 6. 观测与调试

- 记录启动耗时、扫描耗时、复用缓存命中数
- 增加开发期开关，用于输出扫描统计信息
- 必要时给桌面端增加性能调试面板

### 7. 跨平台发布

- 在 `client/package.json` 中补充 macOS 打包目标
- 增加 macOS 所需的 `icon.icns`
- 在 GitHub Actions 中增加 `macos-latest` 构建任务
- 明确是否需要同时产出 `arm64`、`x64` 或 `universal`
- 后续补充 Apple Developer 签名与 notarization 流程
- 评估未签名测试包与正式发布包两套分发方式

## 可选增强

- 改成虚拟列表，进一步降低超大项目库的渲染压力
- 增加搜索索引缓存，避免每次都遍历全部项目字段
- 支持后台文件监听，文件变化后自动更新对应项目

## 备注

- 这一版优先做“性能结构优化”，不优先做界面变化
- 如果后续要继续拆分，可以把 `main.js` 中的扫描与缓存逻辑再抽成单独模块
